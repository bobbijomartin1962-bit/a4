const menu=document.querySelector('.menu'),nav=document.querySelector('header nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.classList.remove('on'));b.classList.add('on');document.querySelectorAll('[data-category]').forEach(c=>c.hidden=b.dataset.filter!=='All'&&c.dataset.category!==b.dataset.filter)}));const cookie=document.querySelector('.cookie');if(localStorage.getItem('ica-consent'))cookie?.remove();document.querySelectorAll('[data-cookie]').forEach(b=>b.addEventListener('click',()=>{localStorage.setItem('ica-consent',b.dataset.cookie);if(b.dataset.cookie==='accept')gtag('consent','update',{analytics_storage:'granted'});cookie.remove()}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('seen')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelector('form')?.addEventListener('submit',e=>{e.preventDefault();e.target.querySelector('.form-note').textContent='Thank you. Your enquiry is ready for the editorial desk.';e.target.reset()});